extends layout

block content
  h1 #{service_name} Bootstrap
  .container(ng-controller="TestApp")
    input(ng-model="me.username")
    textarea(ng-model="data",cols=40,rows=10)
    #users(ng-repeat="user in users")
      .user {{user.id}} - {{user.username}}
    #show {{data}}