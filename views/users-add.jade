block content
  .container
    .block
      form(enctype="multipart/form-data")
        .form-group
          input(type="file", name="picture", accept="image/*")
        .form-group
          label My name
          input.form-control.input-lg(type='email', placeholder="My name")

        button.btn.btn-primary.btn.lg(type='submit') Add me
