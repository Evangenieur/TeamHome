block content
  .container
    .block
      | Coucou
      .chat(ng-repeat="chat_entry in timelines | matchCurrentChannel:current_channel | orderBy:'-date_started'") 
        img(ng-src="{{get_user(chat_entry.user, 'avatar')}}",width=50, height=50)
        {{get_user(chat_entry.user, 'username')}} {{chat_entry.datum.text}}
        br
      br
      input(type="text",ng-model="chat.datum.text",enter-submit="send()")